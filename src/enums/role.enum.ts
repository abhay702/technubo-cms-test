/**
 * RoleEnum
 *
 * Enumeration for different types of roles.
 */
export enum RoleEnum {
  SUPER_ADMIN = "Super Admin", //Super Admin role
  ADMIN = "Admin", //Platform Admin role
}

/**
 * RoleEnumSlug
 *
 * Enumeration for role slugs.
 */
export enum RoleEnumSlug {
  SUPER_ADMIN_SLUG = "super-admin", // Slug for Super Admin role
  ADMIN_SLUG = "admin",
}
